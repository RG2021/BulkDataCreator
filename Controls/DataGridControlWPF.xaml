<UserControl x:Class="Mockit.Controls.DataGridControlWPF"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Mockit.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid x:Name="fieldDataGrid" d:ItemsSource="{d:SampleData ItemCount=5}" AutoGenerateColumns="False" IsReadOnly="False" SelectionMode="Single" ClipboardCopyMode="IncludeHeader" IsTextSearchCaseSensitive="True" CanUserReorderColumns="False" CanUserResizeRows="False" Background="{x:Null}" BorderBrush="#FF9C9C9C" ScrollViewer.CanContentScroll="True" OpacityMask="Black" AlternatingRowBackground="#FFFCFCFC" HeadersVisibility="Column" IsSynchronizedWithCurrentItem="True" IsTextSearchEnabled="True" BorderThickness="1,1,1,1" HorizontalGridLinesBrush="#FFCDCDCD" VerticalGridLinesBrush="#FFA3A3A3" VerticalContentAlignment="Center" MinWidth="200" CanUserDeleteRows="False" CanUserAddRows="False">
        <DataGrid.Resources>
            <Style TargetType="DataGridCell">
                <Setter Property="VerticalContentAlignment" Value="Center"/>
            </Style>
        </DataGrid.Resources>
        <DataGrid.Columns>
            <DataGridCheckBoxColumn CanUserResize="False" CanUserSort="False" CanUserReorder="False" Header="Select" IsReadOnly="False" IsThreeState="False">
                <DataGridCheckBoxColumn.ElementStyle>
                    <Style TargetType="CheckBox">
                        <Setter Property="Focusable" Value="False"/>
                        <Setter Property="HorizontalAlignment" Value="Center"/>
                        <Setter Property="VerticalAlignment" Value="Center"/>
                    </Style>
                </DataGridCheckBoxColumn.ElementStyle>
            </DataGridCheckBoxColumn>
            <DataGridTextColumn Binding="{Binding Field.DisplayName}" CanUserResize="True" Header="Field Name" IsReadOnly="True" CanUserReorder="True" Width="*" MinWidth="50">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="4,0,0,0"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding Field.LogicalName}" CanUserResize="True" Header="Logical Name" IsReadOnly="True" MinWidth="50">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="4,0,0,0"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding Field.DataType}" Header="CRM Type" IsReadOnly="True" Width="*" MinWidth="40">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="4,0,0,0"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTextColumn Binding="{Binding Mock.MockType}" Header="Mock Type" IsReadOnly="True" Width="*" MinWidth="40">
                <DataGridTextColumn.ElementStyle>
                    <Style TargetType="TextBlock">
                        <Setter Property="VerticalAlignment" Value="Center"/>
                        <Setter Property="Padding" Value="4,0,0,0"/>
                    </Style>
                </DataGridTextColumn.ElementStyle>
            </DataGridTextColumn>
            <DataGridTemplateColumn Header="Action" CanUserResize="False" Width="Auto">
                <DataGridTemplateColumn.CellTemplate>
                    <DataTemplate>
                        <Button Content="Mock" Command="{Binding DataContext.RowCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}" HorizontalContentAlignment="Center" Background="{x:Null}" BorderBrush="#FF707070" Grid.IsSharedSizeScope="True" Margin="2,2,2,2" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.CanContentScroll="True" IsDefault="True">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="2">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
            </DataGridTemplateColumn>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
